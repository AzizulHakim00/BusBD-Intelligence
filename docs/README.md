# BusBD Intelligence static frontend

This folder is the GitHub Pages frontend for the BusBD Intelligence repository.

The browser-ready HTML, CSS, JavaScript, favicon, and fonts were recovered from
the public deployment. This is not the original React/TypeScript source or the
Spring Boot backend.
